RULES:
  One and Only Rule:
    CONDITIONS:
      extractor.filesystem.xplat.basename_full: '4123.epub'
    DATA_SOURCES:
      publisher: extractor.filesystem.xplat.basename_full
    NAME_TEMPLATE: '{publisher}'
    exact_match: true

A single rule with a constant file name, I.E. the template does not contain placeholders.
Lack of conditions requires passing '--batch' to avoid confirmation to use rule that scored below the threshold.
